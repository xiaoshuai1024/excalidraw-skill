# dependencies / test artifacts
.test-tmp/
node_modules/

# rendered output (regenerable; keep the committed assets/ examples only via force-add)
*.rendered.svg
*.rendered.png

# OS
.DS_Store
__pycache__/
